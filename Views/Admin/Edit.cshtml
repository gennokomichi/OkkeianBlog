@model OkkeianBlog.Models.Article

@{
    ViewData["Title"] = "記事編集";
}

<head>
    <link href="~/css/editor.css" rel="stylesheet" />
</head>

<h1>@ViewData["Title"]</h1>

<form asp-action="Edit" enctype="multipart/form-data">
    <input type="hidden" asp-for="Id" /> <!-- 記事IDを送信するための hidden フィールド -->

    <div class="form-group">
        <label asp-for="Title" class="control-label"></label>
        <input asp-for="Title" class="form-control" required />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Content" class="control-label"></label>
        <textarea asp-for="Content" id="Content" class="form-control">@Model.Content</textarea>
        <span asp-validation-for="Content" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Author" class="control-label"></label>
        <input asp-for="Author" class="form-control" required />
        <span asp-validation-for="Author" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="PublishDate" class="control-label"></label>
        <input asp-for="PublishDate" type="date" class="form-control" required />
        <span asp-validation-for="PublishDate" class="text-danger"></span>
    </div>

    <!-- 🔹 現在のサムネイルを表示 -->
    @if (!string.IsNullOrEmpty(Model.ThumbnailPath))
    {
        <div class="form-group">
            <label>現在のサムネイル</label><br>
            <img src="@Model.ThumbnailPath?v=@DateTime.Now.Ticks" alt="サムネイル" style="max-width: 200px;">
        </div>
    }

    <!-- 🔹 新しいサムネイル画像をアップロード -->
    <div class="form-group">
        <label for="Thumbnail" class="control-label">新しいサムネイル画像</label>
        <input type="file" id="Thumbnail" name="Thumbnail" class="form-control" />
    </div>

    <button type="submit" class="btn btn-primary">保存</button>
</form>

<a href="@Url.Action("Index", "Admin")">記事一覧に戻る</a>

<script src="https://cdn.ckeditor.com/ckeditor5/40.0.0/classic/ckeditor.js"></script>

<script src="~/js/ckeditor-init.js"></script>
